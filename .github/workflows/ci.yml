name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: read

jobs:
  verify:
    name: Build and test
    uses: ./.github/workflows/verify.yml
    with:
      node-version: '20'
